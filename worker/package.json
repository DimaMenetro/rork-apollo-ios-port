{
  "name": "apollo-engine-worker",
  "version": "1.0.0",
  "description": "Apollo Engine backend — zero-regression port of the four Base44 functions to Cloudflare Workers.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.5",
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.99.0"
  }
}
